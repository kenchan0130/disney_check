name: Nigthly
on:
  schedule:
    - cron: '*/5 * * * *'

env:
  SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

jobs:
  build:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v2
      - run: |
          ./ticket.sh
      - run: |
          ./ikkyu_disney.sh 20201127
      - run: |
          ./ikkyu_disney.sh 20201128
